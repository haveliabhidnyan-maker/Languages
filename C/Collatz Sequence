#include <stdio.h>

int collatz(int n) {
    int count = 0;

    while (1) {
        printf("%d ", n);
        count++;

        if (n == 1)
            break;

        if (n % 2 == 0)
            n = n / 2;
        else
            n = 3 * n + 1;
    }

    return count;
