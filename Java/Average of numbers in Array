
class Solution {
    public String average(int arr[]) {
        float avg = 0;
        float len = arr.length;
        

        for (int i = 0; i < arr.length; i++)
            avg += arr[i];
            float average= avg /len;
            

        return String.valueOf(average);
        
    }
}
